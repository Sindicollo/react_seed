const React = require('react');

var App = React.createClass ({
    render: () => (
        <div>
            <h1>This is React App Seed</h1>
            <p>React, webpack and magic!</p>
        </div>
    ) 
});

module.exports = App;

